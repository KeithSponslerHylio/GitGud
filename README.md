# Installation

Make sure you have python installed. There are no fancy packages you need to install to get this code working. 

# Usage

run 
```
python story_generator.py
```
to generate a story. Edit the source files in the ```stories/``` and ```words/``` dirs to change the generated output.